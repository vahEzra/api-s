function sendSync(boolean)
  if boolean then
    return "Servers Are Up"
  else
    return "Servers Are Down"
  end
end

local a=[[Protected anyway Even though It's in SSS. Not really protected.]]
return sendSync(false)
